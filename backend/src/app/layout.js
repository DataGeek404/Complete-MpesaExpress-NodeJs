export const metadata = {
    title: 'M-Pesa Gateway',
    description: 'M-Pesa Integration Backend',
  }
  
  export default function RootLayout({ children }) {
    return (
      <html lang="en">
        <body>{children}</body>
      </html>
    )
  }
  